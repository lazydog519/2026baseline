> 2026-09-25 完成：100×4 独立求解及400份原版重新评估均通过；以下保留运行和恢复流程供复现。最终指标见 final/aggregate.json，代码包隔离复现已通过。

# 第一问 v5 全量运行

正式代码已冻结，禁止运行途中改 q1_cold.py、q1_cold_config.json、q1_optimized.py、baseline.py、q1_submit.py。每个 case/core 为独立新进程，不用历史答案。

当前命令先运行 q1_experiment.py（full，100例×2/3/4/5核，workers=4），仅成功退出后自动运行 q1_audit.py，输出 q1_cold/final。两者在同一个 PowerShell 会话内串行执行。不能另起重复任务。大例原评估可能耗时二十分钟以上，进程在用 CPU 时先等待。

观察：full/progress.csv；各例的 search.jsonl、process.json、console.log；复核阶段 final/audit_progress.csv。只有 final/aggregate.json 的 complete_cases=100、missing_jobs=0、rechecked_jobs=400 才能视为全量复核完成。

若进程确已退出且区间不全，先查错误，不重启已完成任务。可用 q1_experiment.py 的 --resume，同一输出目录、--sample full --cores 2 3 4 5 --workers 4；它核对当前输入、参数、方案哈希及成功记录，仅跳过完整实验，未完成任务仍从当前图冷启动。该恢复功能已在10个确认例上验证：跳过10组，新评估0次。绝不在原进程仍活着时重复启动。

完成复核后用同一Python顺序运行 solution/q1_package.py --project . 和 solution/q1_figures.py --project .。只生成两张PNG及对应PDF，查看图像并修正排版（不改数据）。检查 技术思路稿-问题一独立求解.md 的自动结果段、模型与算法公式.tex、package_verification.json。

最后用 stage_github_snapshot.py 同步A题材料至 D:/huawei_a_2026/github_baseline，remote为 https://github.com/lazydog519/2026baseline.git（已更名）。原题A目录与之前相同。fetch并保留用户的修改；检查 snapshot complete、100MB限制、official和frozen代码原始字节，再add/commit/push，不输出凭据。两处 solution/ 和 project/solution/ 都需与冻结哈希一致。原有.gitattributes对这些路径及project/official/禁用换行转换，勿改成全仓库文本策略。避免全库git diff --check导致原附件CRLF巨量输出。

上传验证 git ls-remote origin refs/heads/main 等于本地 HEAD 后关闭监控 a。最后报告全量均值、未达到目标时如实说明，并给出代码包、技术稿、公式和两张图的链接。
